#include <stdio.h>
int main() {
    int x,y,z;
    scanf("%d %d %d",&x,&y,&z);
    if (a==b && a==c){
        printf("Equilateral");
    }
    else if(a==b && a!=c){
        printf("Isosceles");
    }
    else {printf(Scalene);}
    return 0;
}